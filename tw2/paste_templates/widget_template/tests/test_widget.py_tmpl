from tw2.core.testbase import WidgetTest
from tw2.${package} import *

class Test${package.capitalize()}(WidgetTest):
    # place your widget at the TestWidget attribute
    widget = ${package.capitalize()}
    # Initilization args. go here 
    attrs = {'id':'${package}-test'}
    params = {}
    expected = """<div id="${package}-test"></div>"""
